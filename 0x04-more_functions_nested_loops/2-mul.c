#include "main.h"

/**
 * mul - function that multiplies two integers.
 * @a: first input
 * @b: second input
 * Return: the result of multiplition two integers.
 */
int mul(int a, int b)
{
	return (a * b);
}
